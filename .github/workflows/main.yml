name: Test Workflow

on:
  workflow_dispatch:

jobs:
  node:
    name: Setup Node
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version-file: '.nvmrc'
      - run: npm --version
      - run: node --version
